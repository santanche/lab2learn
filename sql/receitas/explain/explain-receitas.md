~~~sql
EXPLAIN
SELECT * FROM RECEITA;
~~~

~~~sql
EXPLAIN
SELECT R.NOME_RECEITA
FROM RECEITA R
WHERE R.CODIGO_RECEITA = 'BOLOCE';
~~~

~~~sql
EXPLAIN
SELECT R.NOME_RECEITA
FROM RECEITA R
WHERE R.NOME_RECEITA = 'Bolo Cenoura';
~~~

~~~sql
EXPLAIN ANALYZE
SELECT R.NOME_RECEITA
FROM RECEITA R
WHERE R.NOME_RECEITA = 'Bolo Cenoura';
~~~